# Verify's that the data passed in has all the required fields, and fixes any missing ones


def verify_data(data: dict, type: type):
    from models.mini_widget import MiniWidget
    from models.widget import Widget
    
    if type is MiniWidget:
        pass
    pass