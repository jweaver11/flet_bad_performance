import flet as ft


# Function to remove our object from the story, and from its reference pin location

# Needs to pass through the object, so we grab title, pin location, and tag
# make sure they match then delete